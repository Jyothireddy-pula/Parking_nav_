# VIT-AP GPS survey log

**Status: not yet conducted.** No entries below are real; this is the
template to fill in once the physical walk happens. Every walk session
gets one entry. Do not backfill entries for a walk that didn't happen, and
do not fill in placeholder values "to complete the table" — an empty log
is more honest than a fabricated one.

## Entry template

Copy this block per walk session:

```
### <date, e.g. 2026-XX-XX>

- Surveyor(s): <name(s)>
- Device: <phone model / GPS app + version, e.g. "Pixel 7, Google Maps app" or "OnePlus Nord, GPX Logger v3.2">
- Areas covered: <e.g. "Main Gate to Academic Block 1, North Gate to Library">
- Entities recorded: <gate IDs / road IDs / parking lot IDs / destination IDs captured this session>
- Raw export file(s): <filename(s) in raw_exports/, exactly as exported>
- Known issues: <e.g. "GPS drift near Block 3 due to tree cover, corrected against satellite view for points X, Y">
- Satellite verification done by: <name>, <date>
```

## Log

_(no entries yet — see status above)_
